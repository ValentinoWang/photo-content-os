# OpenClaw 开发清单审核整改实施进度

本文件是机器节点的运行投影，不是决定或依赖权威。权威来源为 `.ssot/manifest.json`、`.ssot/planning-compiler.json`、`.ssot/nodes/` 和 `.ssot/edges/`。

## 当前状态

- 计划版本：3
- 依赖图版本：2
- 接口冻结版本：3
- 源码基线：`4737e45525c2cb9359bfb02952d7b690b6799761`
- 远端主分支基线：`origin/main@4737e45525c2cb9359bfb02952d7b690b6799761`
- 已接受节点：8 / 27（AA、A1、D1、D2、D3、D4、D5、D6）
- 当前就绪节点：7 / 27（A2、A3、B1、B3、C1、E1、E4）
- 阻塞节点：12 / 27
- 已注册 Codex 执行进程：0
- 已实施业务代码：0 个节点
- 已正式验收发布切片：0 / 4
- 尚未拍板：无

## 已接受决定

| 决定 | 2026-09-02 的正式结论 | 直接消费者 |
|---|---|---|
| D1 | `06_edit_decision_list.json` 是机器执行唯一权威；Studio 文本只保存创作说明和只读摘要 | C1 |
| D2 | 生产生成删除建议；用户选择确认后才移入 macOS 废纸篓，禁止自动或永久删除 | B2、E2 |
| D3 | 创意模型由用户配置，支持 Codex/OpenAI、Claude/Anthropic、DeepSeek 等提供方 | E1 |
| D4 | ChatCut 仅作为可选第三方集成，通过 ChatCut Desktop 桌面本地模型上下文协议（MCP）接入，展示前必须实时探测 | E6 |
| D5 | 生命周期和物理位置同时配置，用户可选择位置，每个位置独立保存清单、内容校验值和回读状态 | E3 |
| D6 | 优先复用上游中台账号；不存在时在上游同一身份系统创建 | E4 |

## 当前就绪前沿

| 节点 | 下一项可交付结果 | 依赖状态 | 写入冲突 |
|---|---|---|---|
| A2 | 清单事实、统计和状态分类纠偏 | A1 已接受 | 与 A3 共享原始 HTML，最终落盘需串行 |
| A3 | 标准 HTML 文档头、移动视口和决定筛选 | A1 已接受 | 与 A2 共享原始 HTML，最终落盘需串行 |
| B1 | 照片 EXIF、内容 SHA-256、可读状态字段合同 | A1 已接受 | 不触碰原始媒体 |
| B3 | 媒体清单负例与回归测试设计 | A1 已接受 | 必须与 B1 合同一致 |
| C1 | 从结构化剪辑方案读取并桥接桌面接口 | D1 已接受 | 独立于 A2、A3、B1、B3 |
| E1 | 用户可配置的创意模型提供方 | D3 已接受 | 密钥只使用受控引用 |
| E4 | 上游统一身份查询与同系统账号创建合同 | D6 已接受 | 不建立本地账号事实源 |

七条是逻辑开发线，不等于已经启动七个外部进程。当前没有注册 Codex 执行进程；A2 与 A3 可以分别准备，但同一 HTML 的最终写入必须由单一汇编者完成。

## 仍受阻的实现线

- B2 等待 B1；B4 等待 B2 与 B3。
- E2 和 E3 等待 B4；两者随后可以并行。
- E5 等待 E4。
- E6 的 D4 决定已接受，仅等待 C4。
- D7 等待 A4、B4、C4 与 E1-E6 全部接受；D8 只消费 D7 的不可变候选。

## ChatCut 正式决定

用户/产品负责人已接受 D4：ChatCut 仅作为可选第三方集成，正式接入方式是 ChatCut Desktop 桌面本地模型上下文协议（MCP）。只有实时探测成功且用户明确连接后才显示可用；不加入 OpenClaw 内建编辑后端集合，不成为第二权威，不调用未公开接口。官方证据见 `chatcut-official-evidence.md`。

## 证据边界

本次只更新 SSOT 机器源、生成视图、决定状态和调查证据。尚未修改原始清单、原型或业务源码，尚未执行 E1-E6 的实现测试，也没有 ChatCut 实际连接、上游账号、模型接口、云盘、移动硬盘、生产或物理设备回读。Obsidian 快照通过时只证明声明文档的复制和哈希一致。

## 本次验证结果

- 复杂度、机器节点与依赖、生成视图、结构、中文可读性和并行分析均通过。
- 当前机器计算的就绪宽度为 7，全图最大反链宽度为 10，关键路径为 7 条边。
- 仓库安全检查通过。
- 目标 Obsidian 快照校验通过，只复制 1 份声明主文档，不包含未决问题文件。全局归档审计失败关闭：另一个项目的 `Harness_Engineering/manual-acceptance-log-v0-1` 快照清单与当前源文件哈希不一致；本次没有越界刷新该无关快照，因此整体归档完成状态为部分完成。
- 统一验证入口中的 bundle 检查全部通过，但运行技能溯源因仓库缺少 `.harness/manifest.yaml` 失败；该文件不属于本 SSOT 修订范围，因此没有越界创建。完整回执见 `.ssot/validation-report.json`。

## 下一次状态迁移

1. 可从 C1、E1、E4 中选择互不写冲突的实现线，与 A2/A3、B1/B3 同步推进。
2. B1 与 B3 接受后推进 B2 和 B4，再并行解锁 E2、E3。
3. D4 已接受；C4 接受后即解锁 E6。
4. 每个发布切片只在真实代码、目标命令、候选身份和失败回退均可回读后进入接受状态。
