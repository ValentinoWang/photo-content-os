# OpenClaw 开发清单审核整改实施进度

本文件是机器节点的运行投影，不是决定或依赖权威。权威来源为 `.ssot/manifest.json`、`.ssot/planning-compiler.json`、`.ssot/nodes/` 和 `.ssot/edges/`。

## 当前状态

- 计划版本：5
- 依赖图版本：3
- 接口冻结版本：4
- 源码基线：`61f1c85`；发布候选为 `7dd8e4cf4b4e5e9b6626d201c878cb06def8dc53`。
- 远端主分支：通过系统代理推送并回读为 `origin/main@7dd8e4cf4b4e5e9b6626d201c878cb06def8dc53`。
- 已接受节点：28 / 28。
- 当前就绪节点：0 / 28。
- 已注册并结束的受控 Codex 执行进程：7（B2、E1、E2、E3、E4、E5、E6）；提示词均已删除，日志、结构化返回和台账保留。
- 已接受发布切片：4 / 4（R1、R2、R3、R4）；R4 以本地运行门禁、GitHub `main` 候选和远端回读接受，不扩大为外部系统或生产验收。
- 尚未拍板：无

## 已接受决定

| 决定 | 2026-09-02 的正式结论 | 直接消费者 |
|---|---|---|
| D1 | `06_edit_decision_list.json` 是机器执行唯一权威；Studio 文本只保存创作说明和只读摘要 | C1 |
| D2 | 生产生成删除建议；用户选择确认后才移入当前操作系统的回收站，禁止自动或永久删除 | B2、E2 |
| D3 | 创意模型由用户配置，支持 Codex/OpenAI、Claude/Anthropic、DeepSeek 等提供方 | E1 |
| D4 | ChatCut 仅作为可选第三方集成，通过 ChatCut Desktop 桌面本地模型上下文协议（MCP）接入，展示前必须实时探测 | E6 |
| D5 | 生命周期和物理位置同时配置，用户可选择位置，每个位置独立保存清单、内容校验值和回读状态 | E3 |
| D6 | 用户主动配对时优先复用上游中台账号；未登录、未配对或系统不支持时 Studio 保持本地全功能 | E4、E5 |

## 发布结论

所有实现节点和 D7/D8 已接受。E2、E3、E5、E6 已以四个独立监督器进程并行运行，启动屏障和进程编号记录在 `execution/artifacts/wave-e2-e3-e5-e6-launch.json`；其余受控执行线的独立日志和回执也已留档。候选 `7dd8e4cf` 已推送并由 `ls-remote` 回读；其直接回退点为 `61f1c85`。

## ChatCut 正式决定

用户/产品负责人已接受 D4：ChatCut 仅作为可选第三方集成，正式接入方式是 ChatCut Desktop 桌面本地模型上下文协议（MCP）。只有实时探测成功且用户明确连接后才显示可用；不加入 OpenClaw 内建编辑后端集合，不成为第二权威，不调用未公开接口。官方证据见 `chatcut-official-evidence.md`。

## 证据边界

本次更新 SSOT 机器源、生成视图、清单、原型、源码证据和实现/测试门禁。E1-E6 已通过临时夹具或注入适配器的本地测试；没有 ChatCut 实际连接、上游账号、模型接口、云盘、移动硬盘、生产或物理设备回读。Obsidian 快照通过时只证明声明文档的复制和哈希一致。

## 本次验证结果

- 复杂度、机器节点与依赖、生成视图、结构、中文可读性和并行分析均通过。
- 新增静态清单门禁和临时 Inbox 提升夹具门禁均通过；原 R2 模块命令已复现为失败，发现式 R2 命令通过。
- R1、R2、R3 和 R4 的针对性发现式测试均已通过；候选前全量发现式测试为 304 项通过，R1 静态与 R4 整合复跑为 6 项通过。R4 覆盖模型配置、废纸篓恢复、归档位置、上游身份/会话、ChatCut 探测与 Studio 整合。
- 无项目状态的 Studio 设置页已在 `390x844` 窄屏运行验收：根文档无横向溢出、无控制台警告或错误，删除入口只在项目已选择时显示。D7 回执见 `execution/artifacts/D7/ledger/D7-final-local-verification.json`。
- 七个受控 Codex 执行线已结束，其中 E2、E3、E5、E6 有真实的四进程并行启动记录；调度模型保持事件驱动。
- 仓库安全检查通过。
- GitHub `main` 已回读候选 SHA；本地/远端/worktree 只保留 `main`。本任务的 Studio 服务和浏览器页已关闭，临时状态目录已移入系统废纸篓。D8 回执见 `execution/artifacts/D8/ledger/D8-release-decision.json`。
- 当前 SSOT 已完成 Obsidian 定向快照与 `--check`；全局归档审计也已通过，共核验 40 个归档包和 23 份待决文档。审计发现的两处无关漂移均已从各自权威源刷新，未使用豁免。
- 历史统一验证中的运行技能溯源失败因缺少 `.harness/manifest.yaml`；它不是本 SSOT 的权威输入。最终只依据本次新鲜的仓库门禁、SSOT 门禁和归档审计，不将旧失败报告误写为本次结果。

## 剩余边界

无待执行节点。D8 已绑定本次主文档的 Obsidian 快照、`--check` 与全局 `--audit-archive`；剩余操作只是推送本次 D8 文档提交并重新回读 `origin/main`。
